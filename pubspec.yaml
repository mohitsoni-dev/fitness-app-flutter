name: fitness_app_flutter
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  google_nav_bar: ^5.0.5
  line_icons: ^2.0.1
  flutter_animation_progress_bar: ^2.0.1
  shared_preferences: ^2.0.7
  numberpicker: ^2.1.1
  appbar_animated: ^0.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true


